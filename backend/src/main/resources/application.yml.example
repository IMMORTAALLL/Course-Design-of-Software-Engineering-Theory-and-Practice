spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock_forum?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password

jwt:
  secret: replace-with-local-secret
  expiration: 86400

server:
  port: 8080
